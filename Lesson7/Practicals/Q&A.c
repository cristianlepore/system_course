#include <stdio.h>
#define N 5

int main(){
    int a[N], i;
    /* Answer to question #3 */
    i[a] ==> a[i]

    /* demonstration */
    a[i] = *(a + i) ==> addition is commutative ==>a
    *(i + a) ==> i[a]

    return 0;
}