#include <stdio.h>
typedef char boolean;
#define FALSE 0
#define TRUE 1
typedef enum {
    Red, Orange, Yellow, Green, Blue, Violet
} Color;
enum weather {rain, snow=2, sun=4};

int main(){
    check = (x > 0 );
    if(check == TRUE){

    }

    return 0;
}