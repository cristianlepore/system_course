- In C you build compiler for a specific architecture (OS or computer). In Java we have the JVM.
- Can the type 'char' be interpreted as a number?
- What is the range of char and how many bytes do they require in a normal architecture?
- What is the range of int and how many bytes do they require in a normal architecture?
- What happen if you type the line: printf("%d", 'a');? and why?
- How does the scanf work?
- Difference between implicit and explicit casting.
- Does C has boolean numbers?
- Enumerated types.
- Data struncture: array[]? Explain.
- Struct and Union
- Is C an object oriented language?
- How can I declare a variable?
- What is the & operator?
- What is the meaning of Wall when used in the command line?
- How can you write a for code?
- Switch statement - What is the meaning of the break instruction?
- Switch statement - what happen if I do not use the break instuction?
- How many statement do you know? While, do-while, for
- Meaning of the continue instruction.
- What does it mean if I use a break instruction inside a for loop?
