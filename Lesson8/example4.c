#include <stdio.h>

int main()
{
    char planets[][8] = {"Giove", "nettuno", "plutone"}; /* 2-D array */
    char *planets[] = {"...", "..."};                    /* better for memory usage because I use pointer to mono dimensional arrays */

    return 0;
}